data = pd.read_csv("dataset_T1.csv") \
         .loc[:, ["marker", "a_1311", "a_1592", "a_2713", "a_8006", "a_18178", "tag"]] \
         .query("marker % 2 == 0") \
         .query("marker != 8") \
         .reset_index(drop=True)

data.marker = data.marker.map({j:i+1 for i, j in enumerate(data.marker.unique())})

data.columns = ["position", "anchor_1", "anchor_2", "anchor_3", "anchor_4", "anchor_5", "tag"]

data